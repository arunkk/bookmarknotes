node_modules/
.env
.env.*
.DS_Store

# Raw source dumps. Deliberately never committed: slack-links.json holds the
# URLs the importer refused as unpublishable (internal mirage.percipient.ai
# hosts, an EC2 hostname, private Google Docs, assistant session links), and
# git history would keep them public even after a delete.
data/inbox/*
!data/inbox/.gitkeep

# Private overlay: merged client-side, never published
docs/data/bookmarks.local.json

# Scratch
*.log
tmp/

# Browser-validation artifacts (SPEC.md §7)
.playwright-mcp/
*-live-*.png
bn-*.png

